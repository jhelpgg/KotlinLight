package fr.jhelp.kotlinLight

/**
 * It represents an Objective-C null, it don't have sens on Swift, but here because some library (like `Firebase`) still use it
 *
 * Need to import swift `Foundation` when use
 */
object NSNull
